# Scale sweep: vary sequence_length, others at default
# Use with a script that reads this and generates one dataset per length.

seed: 42

sequence_lengths: [64, 128, 256, 512, 1024]
vocabulary_size: 1024
batch_size: 32

num_classes: 10
task_family_count: 4
num_operations: 8

input_noise_rate: 0.0
label_noise_rate: 0.0
eval_dropout: 0.0

output_dir: "data/sweep_scale"
num_batches: 50
